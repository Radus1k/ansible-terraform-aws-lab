---
# Install with:  make deps   (ansible-galaxy collection install -r ansible/requirements.yml)
collections:
  - name: amazon.aws
    version: ">=8.0.0"
  - name: community.docker
    version: ">=4.2.0"
